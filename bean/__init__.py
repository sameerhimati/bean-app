"""Bean — human-in-the-loop email triage + reply-drafting agent for one support operator.

The package is delivery-agnostic: the spine (contract, sources, llm, routing, loop,
corrections) runs identically under a local Mac app (v1) or a hosted service (v2+).
"""
